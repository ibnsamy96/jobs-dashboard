import { FirstCharSmallPipe } from './first-char-small.pipe';

describe('FirstCharSmallPipe', () => {
  it('create an instance', () => {
    const pipe = new FirstCharSmallPipe();
    expect(pipe).toBeTruthy();
  });
});
